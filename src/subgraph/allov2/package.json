{
  "name": "allov2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ allokate/allov2",
    "create-local": "graph create --node http://localhost:8020/ allokate/allov2",
    "remove-local": "graph remove --node http://localhost:8020/ allokate/allov2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 allokate/allov2",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.64.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
